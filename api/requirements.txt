flask==2.3.3
pandas>=2.1.0
numpy>=1.26.0 
scikit-learn==1.3.0
flask-cors==4.0.0
gunicorn==21.2.0
xgboost==1.7.3
joblib==1.3.1
requests==2.32.5

# numpy==1.24.4 scikit-learn==1.2.2
# python-3.11.9

# Steps to run the BE
# 1.1. Install Python 3.11.x
# Why: Ensures compatibility with all required packages.

# Download and install Python 3.11.x from https://www.python.org/downloads/ 
# 3.11.9 is good to go 

# NOTE:-
# type all these commands inside the api directory  
# Eg(path) :- C:\Users\asraa\OneDrive\Desktop\GIT\notchln_govdocs\api
# Dont skip any steps , follow the flow 


# 2.2. Create a virtual environment
# Why: Keeps project dependencies isolated from global Python packages.
# command- python -m venv venv

# 3.Activate the virtual environment
# Why: Ensures all Python commands use the isolated environment.
# command- .\venv\Scripts\Activate

# 4.Upgrade pip, setuptools, and wheel
# Why: Ensures you have the latest tools for installing packages.
# command- python -m pip install --upgrade pip setuptools wheel

# 5. Install all required Python packages
# Why: Installs all dependencies listed in requirements.txt.
# Command - pip install -r api\requirements.txt

# 6.Retrain the models
# Why: Generates the model files needed for the backend to work.
# (give these both commands one after another not at the same time )
# Command- python api\train_service_completion_model.py
#          python api\train_staffing_model.py

# 7.Start the backend server (Flask app)
# Why: Runs the API server on port 5000.
# Command- python api\app.py

# then open a new terminal and run the FE as usual 

